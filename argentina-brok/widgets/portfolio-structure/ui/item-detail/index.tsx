import type { AssetTypesSectionProps } from '@/shared/types/global.types';

import css from './index.module.css';

export const ItemDetailSection = ({ title, items }: AssetTypesSectionProps) => {
	return <div className={css.root}></div>;
};
